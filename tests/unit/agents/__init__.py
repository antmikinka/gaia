"""
Unit tests for GAIA agents base components.

This package contains unit tests for:
- ToolRegistry: Thread-safe singleton for tool registration
- AgentScope: Per-agent tool scoping with allowlist filtering
- ExceptionRegistry: Tool execution exception tracking
- Backward compatibility shims for legacy code
"""
